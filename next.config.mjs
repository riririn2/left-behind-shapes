/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/left-behind-shapes",
    images: {
        domains: ['i.imgur.com'],
    }
};

export default nextConfig;
