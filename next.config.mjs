/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/left-behind-traces",
    images: {
        domains: ['i.imgur.com'],
    }
};

export default nextConfig;
